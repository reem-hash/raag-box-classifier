# 🏗️ RAAG System Architecture

## High-Level Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    DRIVER / USER                             │
│              (Gradio UI / WhatsApp)                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Uploads Box Image
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   FastAPI Backend                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  POST /upload_box                                     │  │
│  │  POST /feedback                                       │  │
│  │  GET  /statistics                                     │  │
│  │  POST /export_training_data                          │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────┬───────────────────────┬────────────────────────┘
             │                       │
             │                       │ Stores Image
             │                       ▼
             │              ┌────────────────┐
             │              │    Storage     │
             │              │  (Images +     │
             │              │   db.json)     │
             │              └────────────────┘
             │
             │ Calls Classifier
             ▼
┌────────────────────────────────────────────────────────────┐
│              Enhanced Classifier                            │
│  ┌────────────────────────────────────────────────────┐   │
│  │  1. Retrieve Context from RAAG                      │   │
│  │  2. Build Prompt with Examples                      │   │
│  │  3. Call OpenAI Vision API                         │   │
│  │  4. Parse Structured Response                       │   │
│  │  5. Update RAAG Memory                             │   │
│  └────────────────────────────────────────────────────┘   │
└─────────┬──────────────────────────┬───────────────────────┘
          │                          │
          │ Retrieves Context        │ Updates Memory
          │                          │
          ▼                          ▼
┌──────────────────────────────────────────────────────────────┐
│                    RAAG Memory System                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Memory Storage (memory.json)                        │    │
│  │  ├─ box_condition: []                               │    │
│  │  ├─ statistics: {...}                               │    │
│  │  ├─ patterns: {...}                                 │    │
│  │  └─ reinforcement_data: {                           │    │
│  │       high_confidence_correct: [],                  │    │
│  │       low_confidence_cases: [],                     │    │
│  │       model_improvements: []                        │    │
│  │    }                                                 │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Reinforcement Learning Logic                        │    │
│  │  ┌──────────────────────────────────────────────┐  │    │
│  │  │  IF confidence >= 0.85:                      │  │    │
│  │  │    ✅ High Confidence                        │  │    │
│  │  │    → Add to training set                     │  │    │
│  │  │    → Use as future context                   │  │    │
│  │  │    → Trust as ground truth                   │  │    │
│  │  │                                               │  │    │
│  │  │  ELSE confidence < 0.85:                     │  │    │
│  │  │    ⚠️  Low Confidence                        │  │    │
│  │  │    → Flag as edge case                       │  │    │
│  │  │    → Don't use for training                  │  │    │
│  │  │    → Increase drift counter                  │  │    │
│  │  └──────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Drift Detection                                     │    │
│  │  ┌──────────────────────────────────────────────┐  │    │
│  │  │  Monitor: low_confidence_rate                │  │    │
│  │  │                                               │  │    │
│  │  │  IF low_conf_rate > 30%:                     │  │    │
│  │  │    ⚠️  DRIFT DETECTED                        │  │    │
│  │  │    → Log alert                               │  │    │
│  │  │    → Recommend retraining                    │  │    │
│  │  │    → Adjust prompts                          │  │    │
│  │  └──────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────┘    │
└───────────────────────────────┬───────────────────────────────┘
                                │
                                │ When Ready
                                ▼
                    ┌────────────────────────┐
                    │  Training Data Export  │
                    │  (training_data.jsonl) │
                    └───────────┬────────────┘
                                │
                                │ Fine-tune
                                ▼
                    ┌────────────────────────┐
                    │    OpenAI Platform     │
                    │   Fine-Tuning API      │
                    └───────────┬────────────┘
                                │
                                │ Deploy
                                ▼
                    ┌────────────────────────┐
                    │  Enhanced Model        │
                    │  (Higher Accuracy)     │
                    └────────────────────────┘
```

---

## Data Flow Example

### Scenario: Driver uploads damaged box image

```
1. Driver uploads image via Gradio UI
   └─> POST /upload_box

2. FastAPI receives request
   └─> Save image to /images folder
   └─> Call classifier.classify_box_condition()

3. Classifier processes:
   ├─> Retrieve 5 best examples from RAAG
   │   Examples: [
   │     {result: "OK", confidence: 0.95, reason: "..."},
   │     {result: "NEEDS_FIX", confidence: 0.91, reason: "..."},
   │     ...
   │   ]
   │
   ├─> Format context:
   │   "Previous decisions:
   │    1. Result: OK (Confidence: 0.95)
   │       Reason: Minor scuff, structure intact
   │    2. Result: NEEDS_FIX (Confidence: 0.91)
   │       Reason: Visible tear on side"
   │
   ├─> Build prompt with context + new image
   │
   ├─> Call OpenAI Vision API
   │   Response: {
   │     "status": "NEEDS_FIX",
   │     "confidence": 0.88,
   │     "reason": "Large dent on top panel compromising integrity",
   │     "damage_types": ["dent", "structural"]
   │   }
   │
   └─> Update RAAG Memory
       ├─> IF confidence (0.88) >= threshold (0.85):
       │   ✅ Add to high_confidence_correct[]
       │   ✅ Will be used for training
       │   ✅ Will appear in future context
       │
       └─> Check drift
           └─> Low conf rate: 12% < 30% ✅ OK

4. Store result in db.json
   {
     "image": "img_123.jpg",
     "condition": "NEEDS_FIX",
     "confidence": 0.88,
     "reason": "...",
     "damage_types": ["dent", "structural"]
   }

5. Return to UI
   └─> Display: "🔴 BOX CONDITION: NEEDS_FIX"
       └─> Confidence: 88%
       └─> Reason: Large dent on top panel...
```

---

## Memory Evolution Over Time

```
Day 1: 10 evaluations
├─> 7 high confidence (70%)
├─> 3 low confidence (30%)
├─> Drift score: 0.30 (⚠️ at threshold)
├─> Training samples: 7
└─> Status: Keep collecting

Day 3: 50 evaluations
├─> 43 high confidence (86%)
├─> 7 low confidence (14%)
├─> Drift score: 0.14 (✅ healthy)
├─> Training samples: 43
└─> Status: Not enough samples

Day 7: 100 evaluations
├─> 87 high confidence (87%)
├─> 13 low confidence (13%)
├─> Drift score: 0.13 (✅ excellent)
├─> Training samples: 87
└─> Status: ✅ Ready for fine-tuning!

Export → training_data.jsonl (87 samples)
Fine-tune → New model with specialized knowledge
Deploy → Improved accuracy
```

---

## Feedback Loop Integration

```
┌─────────────────────────────────────────┐
│  Driver notices mistake:                 │
│  System said "OK" but box was damaged   │
└───────────────┬─────────────────────────┘
                │
                │ Clicks "Actually NEEDS_FIX"
                ▼
      ┌──────────────────────┐
      │  POST /feedback       │
      │  correct_label=       │
      │  "NEEDS_FIX"         │
      └──────────┬────────────┘
                 │
                 ▼
      ┌──────────────────────────┐
      │  RAAG Memory Update      │
      │  confidence = 1.0        │
      │  (Human = 100% correct)  │
      │  feedback = true         │
      └──────────┬───────────────┘
                 │
                 │ Immediately available
                 ▼
      ┌──────────────────────────┐
      │  Next classification     │
      │  uses corrected example  │
      │  in context              │
      └──────────────────────────┘
```

---

## Statistics Dashboard Flow

```
User clicks "Refresh Stats"
         │
         ▼
GET /statistics
         │
         ▼
┌─────────────────────────────────┐
│  Calculate Metrics:              │
│  ├─ Total evaluations: 150      │
│  ├─ Avg confidence: 0.87        │
│  ├─ Low conf rate: 12%          │
│  ├─ Drift score: 0.12           │
│  ├─ Training samples: 87        │
│  └─ Recent trend: improving     │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│  Check Retraining Status:        │
│  IF samples >= 50 AND           │
│     drift > 0.3:                │
│       retraining_recommended    │
└──────────────┬──────────────────┘
               │
               ▼
         Return JSON
               │
               ▼
┌─────────────────────────────────┐
│  Display in UI:                  │
│  📊 System Statistics            │
│  Total: 150                     │
│  Avg Confidence: 87%            │
│  Low Conf Rate: 12%             │
│  Training Samples: 87           │
│  🔄 Ready for fine-tuning!      │
└─────────────────────────────────┘
```

---

## Component Interactions Summary

```
┌──────────────┐     reads      ┌──────────────┐
│   Gradio UI  │───────────────>│  FastAPI     │
└──────────────┘                └──────┬───────┘
                                       │
                                       │ uses
                                       ▼
                                ┌──────────────┐
                                │  Classifier  │
                                └──────┬───────┘
                                       │
                           ┌───────────┼───────────┐
                           │           │           │
                      retrieves    updates     stores
                           │           │           │
                           ▼           ▼           ▼
                    ┌──────────┐ ┌──────────┐ ┌──────────┐
                    │   RAAG   │ │   RAAG   │ │ Storage  │
                    │  Memory  │ │  Memory  │ │          │
                    └──────────┘ └──────────┘ └──────────┘
                           │           │           │
                           └───────────┴───────────┘
                                       │
                                  all persist
                                       ▼
                                ┌──────────────┐
                                │  File System │
                                │  memory.json │
                                │  db.json     │
                                │  images/     │
                                └──────────────┘
```

---

## Success Metrics

### Healthy System
```
✅ Average confidence: >85%
✅ Low confidence rate: <20%
✅ Drift score: <0.3
✅ Training samples: Growing steadily
✅ Recent trend: Stable or improving
```

### Needs Attention
```
⚠️  Average confidence: <80%
⚠️  Low confidence rate: >30%
⚠️  Drift score: >0.3
⚠️  Training samples: Not growing
⚠️  Recent trend: Declining
```

### Action Required
```
🔴 Average confidence: <70%
🔴 Low confidence rate: >40%
🔴 Drift score: >0.5
🔴 Multiple days of declining performance
→ IMMEDIATE RETRAINING REQUIRED
```
